# setup.py
from setuptools import setup, find_packages

setup(
    name="emotion_recognition_rag",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Voir requirements.txt
    ],
)